
new rl(`rho:registry:lookup`), PoSCh in {
  rl!(`rho:rchain:pos`, *PoSCh) |
  for(@(_, PoS) <- PoSCh){
    new deployerId(`rho:rchain:deployerId`), bondAck in {
      @PoS!("bond", *deployerId, 100, *bondAck)
    }
  }
}