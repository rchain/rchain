stages:
  - test
  - deploy

test:deploy-rnode-to-dockerhub:
  stage: test 
  tags:
    - preloaded-ubuntu1804 
  script:
    - echo $DOCKER_PASSWORD | sudo docker login -u $DOCKER_USERNAME --password-stdin
    - sudo docker pull apline
