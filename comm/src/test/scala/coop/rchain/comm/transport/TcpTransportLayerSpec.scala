package coop.rchain.comm.transport

import org.scalatest.WordSpec

class TcpTransportLayerSpec extends WordSpec {

  "TcpTransportLayer" when {
    "grpc server is already started" should {
      "result in an error" in {
        pending
      }
    }
  }

}
