syntax = "proto3";

package cooop.rchain.node;

service Repl {
  rpc Run (ReplRequest) returns (ReplResponse) {}
}

message ReplRequest {
  string line = 1;
}

message ReplResponse {
  string output = 1;
}