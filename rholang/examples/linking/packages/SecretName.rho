export SecretName in {
  contract SecretName(return) = {
    new privateName, condition in {
      contract condition(evidence, return) = {
        match evidence == privateName with true => { return(true) }
        _ => { return(false) }
      } |
      return([privateName, condition])
    }
  }
}