new x, y, stdout(`rho:io:stdout`) in {

  stdout!({
    "nth for byte array": "deadbeef".hexToBytes().nth(3),
    "expected": "ef".hexToBytes()
  })
  |
  stdout!({
    "length for byte array": "deadbeef".hexToBytes().length(),
    "expected": 4
  })
}
