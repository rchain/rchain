new blockData(`rho:block:data`), stdout(`rho:io:stdout`), retCh in {
  for (@blockNumber, @timestamp, @sender <- blockData !?()){
    stdout!({"block number": blockNumber}) |
    stdout!({"block time": timestamp})|
    stdout!({"block sender": sender})
  }
}
