// A contract that logs the current time to a two files

new time_contract in {
	contract time_contract(stream) = {
        log-time(stream)
	} |
	time_contract!(ostream-new("test.txt")) |
	time_contract!(ostream-new("test2.txt"))
}
