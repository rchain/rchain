// A persistent send demonstrates that even though a receive matched, the send persists

// A successful run will yield a persistent send, while the listen is
// rewritten as a send on the channel @"success":
// @Nil!!("sucess") |
// @"success"!("success")


@Nil!!("sucess") |
for( x <- @Nil ){ x!("success") }
