// Wildcard match
// x and _ are inherently different, and thus should
// not match.

// A successful run of the following code will evaluate
// to nothing (it will rewrite to the Nil process).

match for(x <- @Nil){ Nil } {
        for(_ <- @Nil){ Nil } => { @Nil!(Nil) }
 }
